o texto dentro do noe a coluna fica dentro da tag th

o texto dentro  das celulas de dados do corpo da tabela quando a tag th fica dentro de uma tag tr

em uma tabela criada de forma correta tanto a tag td quando a tag fica dentro do th